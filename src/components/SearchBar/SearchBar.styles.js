import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainContainer: {
        margin: 10,
    },
    searchBox: {
        backgroundColor: '#ececec',
        borderRadius: 10,
        paddingLeft: 20,
    },
});